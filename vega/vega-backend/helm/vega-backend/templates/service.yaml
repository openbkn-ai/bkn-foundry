apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.moduleName }}-svc
  namespace: {{ .Values.namespace }}
  labels:
    module: {{ .Values.moduleName }}
spec:
  selector:
    module: {{ .Values.moduleName }}
  type: {{ .Values.service.vegaBackend.type }}
  ports:
    - name: {{ .Values.moduleName }}
      port: {{ .Values.service.vegaBackend.port }}
      protocol: TCP
      targetPort: backend
      {{- if eq .Values.service.vegaBackend.type "NodePort" }}
      nodePort: {{ .Values.service.vegaBackend.nodePort }}
      {{- end }}