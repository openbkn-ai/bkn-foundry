# GitHub Personal Access Token — collect 命令克隆代码仓库时使用
# 若 source_code/<service> 目录已存在则跳过克隆，无需设置
MY_PAT=github_pat_xxxx

# verify_rds_config.yaml 路径 — verify 命令连接测试数据库时使用
# 默认值: server/verify/rds/verify_rds_config.yaml
VERIFY_RDS_CONFIG=/path/to/verify_rds_config.yaml
