name: release-vega-kafka-connect

on:
  push:
    tags:
      - 'v*'
    branches: ['**']
    paths:
      - 'vega/kafka-connect/**'
      - '.github/workflows/release-vega-kafka-connect.yml'
      - '.github/workflows/reusable-*.yml'
      # 版本冻结提交（release/<x.y.z> 分支）触发全量重建
      - 'VERSION'
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish to GHCR (true) or verify build only (false)'
        type: boolean
        default: false
permissions:
  contents: read
  packages: write

jobs:
  version:
    uses: ./.github/workflows/reusable-version.yml

  verify:
    needs: version
    uses: ./.github/workflows/reusable-release-verify.yml
    with:
      service-path: vega/kafka-connect
      stack: chart-only

  build-kafka-connect:
    needs: [version, verify]
    uses: ./.github/workflows/reusable-container-image.yml
    secrets: inherit
    with:
      service-path: vega/kafka-connect
      dockerfile: docker/Dockerfile
      context: docker
      image-name: kafka-connect
      version: ${{ needs.version.outputs.version }}
      publish: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.publish) }}
      build-args: |
        BASE_IMAGE=bitnamilegacy/kafka:3.9.0-debian-12-r10

  chart-kafka-connect:
    needs: [version, verify, build-kafka-connect]
    uses: ./.github/workflows/reusable-chart.yml
    with:
      service-path: vega/kafka-connect
      chart-path: helm
      chart-name: kafka-connect
      version: ${{ needs.version.outputs.version }}
      publish: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.publish) }}
