name: release-infra-mf-model-manager

# Image FROM ghcr.io/<owner>/model-factory-base:v2 (built by
# release-infra-model-factory-base.yml); that base build must have published first.
# test stack = chart-only: unit tests have pre-existing failures tracked in issue #2.

on:
  push:
    tags:
      - 'v*'
    branches: ['**']
    paths:
      - 'infra/mf-model-manager/**'
      - '.github/workflows/release-infra-mf-model-manager.yml'
      - '.github/workflows/reusable-*.yml'
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish to GHCR (true) or verify build only (false)'
        type: boolean
        default: false
permissions:
  contents: read
  packages: write

jobs:
  test:
    uses: ./.github/workflows/reusable-test.yml
    with:
      service-path: infra/mf-model-manager
      stack: chart-only

  build:
    needs: test
    uses: ./.github/workflows/reusable-build.yml
    secrets: inherit
    with:
      service-path: infra/mf-model-manager
      dockerfile: Dockerfile
      context: '.'
      image-name: mf-model-manager
      version: ${{ needs.test.outputs.version }}
      publish: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.publish) }}

  chart:
    needs: [test, build]
    uses: ./.github/workflows/reusable-chart.yml
    with:
      service-path: infra/mf-model-manager
      chart-path: charts
      chart-name: mf-model-manager
      version: ${{ needs.test.outputs.version }}
      publish: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.publish) }}
