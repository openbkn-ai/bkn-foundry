apiVersion: v1
appVersion: "v26.2.0"
description: Upstream ORY Hydra (v26.2.0, unmodified) on PostgreSQL — the OAuth2/OIDC token engine for the ISF replacement. Pairs with bkn-safe (login/consent/device provider). PostgreSQL backend (hydra's migrations need CAST AS JSON, which MariaDB lacks — MDEV-26448).
name: hydra
version: 0.1.0
