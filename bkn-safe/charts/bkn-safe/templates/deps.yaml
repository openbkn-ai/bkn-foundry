{{- if .Values.bundledDeps.enabled }}
{{- /* Cluster MariaDB creds from config.yaml depServices.rds (platform standard).
       When present, the bkn-safe app DB uses it. */}}
{{- $rds := (.Values.depServices | default dict).rds | default dict }}
# Bundled dependencies: PostgreSQL (hydra's DB — MariaDB can't host hydra) is
# always bundled here and PERSISTED on a PVC. Hydra is upstream ORY v26.2.0.
---
# hydra's signing/encryption key, in its own Secret. Kept separate from the main
# bkn-safe Secret because its value is non-deterministic on a fresh install
# (random) — folding it into a Secret that a deployment checksums would make that
# checksum unstable between the install render and the first upgrade render. It
# is resolved by bkn-safe.hydraSecret: random on a fresh install, preserved
# (never rotated) thereafter.
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-hydra-secrets
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  SECRETS_SYSTEM: {{ include "bkn-safe.hydraSecret" . | quote }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-postgres-data
  namespace: {{ .Release.Namespace }}
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: {{ .Values.bundledDeps.postgresStorage | default "5Gi" | quote }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-postgres
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  # Single-writer DB on an RWO PVC. The default RollingUpdate would surge a new
  # pod before terminating the old one; local-path enforces RWO per-NODE, not
  # per-pod, so both pods can mount the same data dir at once → two postmasters →
  # "data directory lock file is invalid" → corruption / silent data loss (which
  # wipes hydra's schema + OAuth clients and breaks login). Recreate tears down
  # the old pod before starting the new one, so there is never more than one.
  strategy: { type: Recreate }
  selector: { matchLabels: { app: {{ .Release.Name }}-postgres } }
  template:
    metadata: { labels: { app: {{ .Release.Name }}-postgres } }
    spec:
      containers:
        - name: postgres
          image: "{{- if .Values.image.registry }}{{ .Values.image.registry }}/{{ end }}{{ .Values.bundledDeps.postgresImage }}"
          env:
            - { name: POSTGRES_USER, value: "hydra" }
            - { name: POSTGRES_PASSWORD, value: "secret" }
            - { name: POSTGRES_DB, value: "hydra" }
          ports: [{ containerPort: 5432 }]
          volumeMounts: [{ name: data, mountPath: /var/lib/postgresql/data }]
      volumes: [{ name: data, persistentVolumeClaim: { claimName: {{ .Release.Name }}-postgres-data } }]
---
apiVersion: v1
kind: Service
metadata: { name: {{ .Release.Name }}-postgres, namespace: {{ .Release.Namespace }} }
spec:
  selector: { app: {{ .Release.Name }}-postgres }
  ports: [{ port: 5432, targetPort: 5432 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-hydra
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector: { matchLabels: { app: {{ .Release.Name }}-hydra } }
  template:
    metadata: { labels: { app: {{ .Release.Name }}-hydra } }
    spec:
      initContainers:
        - name: migrate
          image: "{{- if .Values.image.registry }}{{ .Values.image.registry }}/{{ end }}{{ .Values.bundledDeps.hydraImage }}"
          command: ["hydra", "migrate", "sql", "-e", "--yes"]
          env:
            - { name: DSN, value: "postgres://hydra:secret@{{ .Release.Name }}-postgres:5432/hydra?sslmode=disable" }
      containers:
        - name: hydra
          image: "{{- if .Values.image.registry }}{{ .Values.image.registry }}/{{ end }}{{ .Values.bundledDeps.hydraImage }}"
          args: ["serve", "all", "--dev"]
          env:
            - { name: DSN, value: "postgres://hydra:secret@{{ .Release.Name }}-postgres:5432/hydra?sslmode=disable" }
            {{- /* Public browser-facing base for issuer + login/consent/device URLs. */}}
            {{- /* Precedence: explicit bundledDeps.publicBaseURL > derived from the */}}
            {{- /* global accessAddress (gateway, shared with onboard) > in-cluster names. */}}
            {{- $pub := .Values.bundledDeps.publicBaseURL }}
            {{- if and (not $pub) .Values.accessAddress .Values.accessAddress.host }}
              {{- $aa := .Values.accessAddress }}
              {{- $scheme := $aa.scheme | default "https" }}
              {{- $port := $aa.port | toString }}
              {{- if or (and (eq $scheme "https") (eq $port "443")) (and (eq $scheme "http") (eq $port "80")) }}
                {{- $pub = printf "%s://%s" $scheme $aa.host }}
              {{- else }}
                {{- $pub = printf "%s://%s:%s" $scheme $aa.host $port }}
              {{- end }}
            {{- end }}
            {{- $svc := printf "http://%s:%v" .Values.image.name .Values.service.ports.port }}
            - { name: URLS_SELF_ISSUER, value: "{{ if $pub }}{{ $pub }}{{ else }}http://{{ .Release.Name }}-hydra-public:4444{{ end }}" }
            - { name: URLS_LOGIN, value: "{{ if $pub }}{{ $pub }}{{ else }}{{ $svc }}{{ end }}/login" }
            - { name: URLS_CONSENT, value: "{{ if $pub }}{{ $pub }}{{ else }}{{ $svc }}{{ end }}/consent" }
            - { name: URLS_DEVICE_VERIFICATION, value: "{{ if $pub }}{{ $pub }}{{ else }}{{ $svc }}{{ end }}/device" }
            {{- /* Post-approval page (replaces hydra's bare fallback). bkn-safe serves it. */}}
            - { name: URLS_DEVICE_SUCCESS, value: "{{ if $pub }}{{ $pub }}{{ else }}{{ $svc }}{{ end }}/device/success" }
            {{- /* Signing/encryption key, resolved by bkn-safe.hydraSecret and
                   stored in the release Secret: random on fresh installs,
                   preserved (never rotated) on upgrades. Read from the Secret so
                   there is a single source of truth. */}}
            - name: SECRETS_SYSTEM
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-hydra-secrets
                  key: SECRETS_SYSTEM
            - { name: OAUTH2_EXPOSE_INTERNAL_ERRORS, value: "true" }
            {{- /* serve all --dev issues cookies without Secure; a SameSite=None */}}
            {{- /* cookie without Secure is dropped by modern browsers, breaking the */}}
            {{- /* device-flow CSRF check ("No CSRF value available in the session */}}
            {{- /* cookie"). The whole browser flow is same-site (one gateway host), */}}
            {{- /* so Lax keeps the cookie on the top-level navigations + form POST. */}}
            - { name: SERVE_COOKIES_SAME_SITE_MODE, value: "Lax" }
          ports: [{ containerPort: 4444 }, { containerPort: 4445 }]
---
apiVersion: v1
kind: Service
metadata: { name: {{ .Release.Name }}-hydra-public, namespace: {{ .Release.Namespace }} }
spec:
  selector: { app: {{ .Release.Name }}-hydra }
  ports: [{ port: 4444, targetPort: 4444 }]
---
apiVersion: v1
kind: Service
metadata: { name: {{ .Release.Name }}-hydra-admin, namespace: {{ .Release.Namespace }} }
spec:
  selector: { app: {{ .Release.Name }}-hydra }
  ports: [{ port: 4445, targetPort: 4445 }]
{{- end }}
