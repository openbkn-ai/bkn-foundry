{
  "input_schema": {
    "type": "object",
    "properties": {
      "response_format": {
        "type": "string",
        "enum": [
          "json",
          "toon"
        ],
        "default": "toon",
        "description": "文本格式：json 或 toon，默认 toon"
      },
      "kn_id": {
        "type": "string",
        "description": "知识网络 ID。也可改用 X-Kn-ID 请求头传入。"
      },
      "query": {
        "type": "string",
        "description": "自然语言问题或关键词。整句直接传，不必拆成字段条件——向量通道吃整句，全文通道由分析器分词。"
      },
      "concept_groups": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "限定召回范围到指定的 BKN 概念分组；不传则在整个知识网络内召回。"
      },
      "max_object_types": {
        "type": "integer",
        "default": 10,
        "minimum": 1,
        "description": "参与实例召回的对象类数量上限。调大能覆盖更多对象类，代价是每多一个对象类多一轮下游查询。"
      },
      "max_instances_per_type": {
        "type": "integer",
        "default": 5,
        "minimum": 1,
        "description": "每个对象类最多返回几条实例。"
      },
      "include_object_types": {
        "type": "boolean",
        "default": true,
        "description": "是否附带命中对象类的精简定义（属性名与类型），默认附带。关掉只在你已经拿着这些对象类的 Schema 时才划算——否则要读懂返回的实例还得再调一次 get_object_types。"
      }
    },
    "required": [
      "query"
    ]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "nodes": {
        "type": "array",
        "description": "命中的实例，按对象类分组返回；组内按相关性降序。跨对象类之间没有全局排序，不要按出现次序判断哪个对象类更相关。",
        "items": {
          "type": "object",
          "properties": {
            "object_type_id": {
              "type": "string",
              "description": "该实例所属对象类 ID，可直接喂给 get_object_types / query_object_instance。"
            },
            "object_type_name": {
              "type": "string"
            },
            "properties": {
              "type": "object",
              "additionalProperties": true,
              "description": "实例属性。字段含义见 search_schema / get_object_types 返回的对象类定义。"
            },
            "score": {
              "type": "number",
              "description": "该实例在其所属对象类内部的相关性分。不同对象类之间不可比。"
            },
            "rerank_score": {
              "type": "number",
              "description": "精排小模型给出的相关性分。仅在部署开启实例精排、且该条被模型打过分时出现。精排只改返回顺序，不改 score——开启后请按返回顺序消费，不要自行按 score 重排。"
            }
          },
          "additionalProperties": true
        }
      },
      "object_types": {
        "type": "array",
        "description": "读懂 nodes 所需的对象类精简定义，**只含真正出了实例的那些对象类**，不是召回扫过的全部。include_object_types=false 时不返回。",
        "items": {
          "type": "object",
          "additionalProperties": true
        }
      },
      "message": {
        "type": "string",
        "description": "仅在没有命中时出现，说明为什么是空的。"
      }
    },
    "required": [
      "nodes"
    ]
  }
}
