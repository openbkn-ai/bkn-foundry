{
  "input_schema": {
    "type": "object",
    "properties": {
      "kn_id": {
        "type": "string",
        "description": "必填。知识网络 ID，须与 search_capabilities 所用一致。工具必须已挂载到该网络才可调用。"
      },
      "toolbox_id": {
        "type": "string",
        "description": "工具箱 ID，取自 search_capabilities 返回的 owner_id。"
      },
      "tool_id": {
        "type": "string",
        "description": "工具 ID，取自 search_capabilities 返回的 capability_id。"
      },
      "arguments": {
        "type": "object",
        "description": "工具的业务入参，按 search_capabilities 返回的 input_schema 填写。不要在这里塞令牌、会话 id 等传输信息，平台会自行携带。"
      }
    },
    "required": [
      "kn_id",
      "toolbox_id",
      "tool_id",
      "arguments"
    ]
  },
  "output_schema": {
    "type": "object",
    "description": "工具的原始响应。函数自身报错同样返回 200，需读响应体判断成败，不能只看调用是否成功。",
    "additionalProperties": true
  }
}
