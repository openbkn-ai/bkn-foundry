# Dependency list for model-factory-base.
# Rebuilds the legacy acr.aishu.cn/ad/model-factory-base:v2 from public sources.
# This base is shared by mf-model-api and mf-model-manager.
# Install in one pip invocation so the resolver sees all constraints at once, especially llmadapter pins.
# Note: AnyRobot (AR) exporter/tlogging has been removed from service code, so the base no longer needs internal wheels.

# --- Internal SDKs, already published to PyPI. ---
llmadapter==1.0.3          # Pulls sqlalchemy/redis/spacy/openai/pandas/pydantic<2/tiktoken<0.4/gptcache/langchainplus-sdk.
DBUtilsX==1.3.3            # dbutilsx.pooled_db (PooledDB/PooledDBInfo), bundled with rdsdriver.
RDSDriver==1.3.3           # rdsdriver for mysql/dm backends.

# --- Service runtime stack, sourced from mf-model-manager/requirements.txt. ---
uvicorn~=0.23.2
fastapi~=0.104.0
pydantic~=1.10.9           # v1, compatible with llmadapter's pydantic<2 constraint.
python-dotenv==0.13.0
openai==0.28.1
coverage==7.3.2
xmlrunner==1.7.7
eventlet==0.33.3
func_timeout==4.3.5
requests==2.31.0
arrow==0.15.8
tzlocal==5.2
pytz==2022.1
datasets==2.18.0
circuitbreaker==2.0.0
APScheduler==3.6.3
urllib3==1.26.7
tenacity==8.3.0
pycryptodome==3.20.0
spacy==3.7.5
transformers==4.38.0
sentencepiece==0.2.0
# tiktoken: do not pin the version. mf-model-manager previously pinned 0.5.1,
#           but llmadapter requires <0.4.0, so let the resolver choose a compatible version.

# --- Metric upload feature group, sourced from mf-model-manager/requirements.txt. ---
absl-py==2.1.0
nltk==3.9.1
jieba==0.42.1
rouge-score==0.1.2
evaluate==0.4.3

# --- Imported by code but not declared upstream; included to complete the base image. ---
confluent-kafka            # Requires system library librdkafka.
aiohttp
sse-starlette
opentelemetry-sdk
opentelemetry-api
opentelemetry-instrumentation-fastapi
